{
  "name": "nuxt-keycloak-sample",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "if [ \"$CI\" != \"true\" ]; then nuxt prepare; fi"
  },
  "dependencies": {
    "nuxt": "^3.12.0",
    "oidc-client-ts": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "sass": "^1.77.0",
    "typescript": "^5.5.0"
  }
}